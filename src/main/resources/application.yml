server:
  port: 8081
Spring:
  cloud:
    config:
      server:
        git:
          default-label: config-server
          uri: https://github.com/franklinconde1/config-service.git
          search-paths: config-data
  application:
    name: config-service